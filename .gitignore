# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — NEVER commit secrets (MASTER_ENCRYPTION_KEY, JWT_SECRET, SMTP creds)
.env
.env*.local
.env.production

# vercel
.vercel

# ---------------------------------------------------------------
# JSON datastore — contains bcrypt hashes and AES-encrypted vault
# data. Never commit. Run `npm run seed` to bootstrap locally.
# ---------------------------------------------------------------
/data/*.json
/data/*.corrupt-*
